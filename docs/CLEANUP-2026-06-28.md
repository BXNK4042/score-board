# Root Directory Cleanup — 2026-06-28

## Goal
Trim tracked baggage and local-only artifacts from the repo root so the project is cleaner for collaborators and `ls` is no longer noisy.

## Method
Every root entry was traced, categorized by impact (cleanliness + repo hygiene + collaborator confusion, weighted by safety of removal), and either untracked, deleted, or left alone.

## Removed

### Tier 1 — Tracked baggage (untracked + deleted)
| Path | Files | Reason |
|------|-------|--------|
| `graphify-out/` | ~110 | Output of the `graphify` skill — fully regenerable, should never be committed. |
| `.agents/` | ~125 | Multi-agent orchestration workspace artifacts (BRIEFING/handoff/progress per subagent). `.gitignore` already listed it; files were committed before the ignore was added. Zero runtime value. |
| `tests/` + `tests/compiled/` | 7 | Playwright suite disabled in `package.json` (`"test:e2e": "echo 'Playwright is disabled for now'"`). Dead until re-enabled. |
| `design/` | 3 | Mockup PNGs not referenced by any code. |

### Tier 2 — Dead/stale files (deleted)
| Path | Reason |
|------|--------|
| `screenshot.mjs` | One-off script referencing a `screenshots/` dir that doesn't exist; not wired into `package.json` scripts. |
| `playwright.config.ts` | Playwright uninstalled; suite disabled. |
| `playwright-report/` | Stale HTML report (already gitignored). |
| `test-results/` | Stale Playwright trace artifacts (already gitignored). |
| `docs/TEST_INFRA.md` | Test-readiness doc for disabled suite. |
| `docs/TEST_READY.md` | Test-readiness doc for disabled suite. |
| `docs/ORIGINAL_REQUEST.md` | Duplicated verbatim inside `.agents/`. |
| `docs/score-history-undo-redo-plan.md` | One-time planning doc; better suited to a PR/issue. |

### Dependency
- `@playwright/test` uninstalled (no longer referenced).

## Changed

### `.gitignore`
- Added `graphify-out/` (other paths already covered or removed).

### `package.json`
- Removed `"test:e2e": "echo 'Playwright is disabled for now'"` script.

## Preserved (not touched)
| Path | Why |
|------|-----|
| `node_modules/`, `.next/`, `.opencode/node_modules`, `tsconfig.tsbuildinfo`, `next-env.d.ts` | Normal regenerable caches; all gitignored; auto-restored by standard tooling. |
| `docs/PROJECT.md` | Active project overview. |
| `public/player-avatar/*.webp` | App assets in use. |
| `app/`, `components/`, `hooks/`, `lib/` | Source code — no changes. |

## Verification
- `npm run lint` — passes (3 pre-existing `<img>` warnings unrelated to this work).
- Working tree and index confirmed via `git status`.

## Net Impact
- **~220 tracked files removed** from the repository.
- **Root `ls` entries: 24 → 16.**
- **Zero impact** on the running app (no source changes).

## To restore anything later
- `graphify-out/` — re-run the `graphify` skill.
- `.agents/` — historical only; no restoration path (was orchestrator scratch space).
- `tests/` — retrieve from git history (`git log --diff-filter=D -- tests/`); reinstall `@playwright/test` and re-add the `test:e2e` script.
- `design/*.png` — `git show HEAD~1:design/Home_Design.png > ...` (or earlier commit if already committed away).
